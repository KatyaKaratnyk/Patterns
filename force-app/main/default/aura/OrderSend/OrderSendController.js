({
    doSend : function(component, event, helper) {
        helper.doSend(component, event, helper);
    },
    doCancel: function (component, event, helper) {
        helper.doCancel(component);
    },
})
